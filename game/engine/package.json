{
  "name": "@arena/engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Element Arena rules engine — deterministic, isomorphic core.",
  "scripts": {
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.9.3"
  }
}
